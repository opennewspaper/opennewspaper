
{if $lang.$message}
    <p class="message">
        {$lang.$message}
    </p>
{/if}
<select name="input[tag]">
    {html_options options=$tags}
</select>

<input type="submit" value="{$lang.delete}" name="action[deleteTag]" id="delete" title="{$lang.deleteSelected}" />
